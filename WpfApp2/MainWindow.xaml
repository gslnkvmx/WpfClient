<Window x:Class="PAC_Man_Game_WPF_MOO_ICT.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PAC_Man_Game_WPF_MOO_ICT"
        mc:Ignorable="d"
        Title="PAC Man Game WPF MOO ICT" Height="800" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="StartSoloGameButton" Content="Start solo Game" Click="StartSoloGameButton_Click" Grid.Row="0" Grid.Column="0"/>
            <Button x:Name="StartDuoGameButton" Content="Start duo Game" Click="StartDuoGameButton_Click" Grid.Row="0" Grid.Column="1"/>
            <!-- Поле для ввода ID игры -->
            <TextBox x:Name="GameIdTextBox" Grid.Row="0" Grid.Column="2" Margin="10" Width="150"/>
            <!-- Кнопка Connect -->
            <Button x:Name="ConnectButton" Content="Connect" Click="ConnectButton_Click" Grid.Row="0" Grid.Column="3" Margin="10,10,10,10"/>
        </Grid>

        <Canvas Name="MyCanvas" KeyDown="CanvasKeyDown" Focusable="True" Background="Black" Margin="-12,10,-387,-734" Grid.Row="1">
            <Label FontSize="20" Name="txtScore" Foreground="White" Content="Score: 0" Canvas.Left="710" />
        </Canvas>
    </Grid>

</Window>